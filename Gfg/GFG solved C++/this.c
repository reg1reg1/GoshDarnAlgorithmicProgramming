#include<stdio.h>
int main()
{  //program to determine endianness of machine
   char *x = "abcd";
   char a[]="abcd"
  // printf("%d %d %d",sizeof(x),sizeof(a),sizeof("abcd"));
   //x= (int *)malloc(sizeof(int));
   //printf("dsadsad\n");
   //fun(a);
   return 0;
}

